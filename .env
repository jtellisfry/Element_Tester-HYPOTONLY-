# ============================================================================
# Element Tester - Workspace Environment Variables
# ============================================================================
#
# This file configures environment variables for the VS Code workspace.
# VS Code and Python extensions automatically read this file.
#
# PYTHONPATH:
# - Tells Python where to find importable packages
# - ./src makes `element_tester` importable from anywhere in the workspace
# - Enables imports like: from element_tester.system.drivers.relay_mcc import ERB08Driver
#
# WHY NEEDED:
# - Without this, Python only searches: current directory, stdlib, site-packages
# - Running scripts from subdirectories (e.g., src/element_tester/system/ui/debug.py)
#   would fail with "ModuleNotFoundError: No module named 'element_tester'"
#
# HOW IT WORKS:
# - VS Code Python extension applies this before running any Python code
# - Relative path ./src resolves from workspace root (where this .env lives)
# - Equivalent to: sys.path.insert(0, "<workspace_root>/src")
#
# HOW TO MODIFY:
# - Keep PYTHONPATH=./src unless you restructure the project
# - If you move element_tester/ out of src/, update path accordingly:
#     PYTHONPATH=./lib             (if moved to lib/element_tester/)
#     PYTHONPATH=./src:./lib       (to include multiple paths; use : on Linux/Mac, ; on Windows)
# - To add multiple paths on Windows:
#     PYTHONPATH=./src;./other_packages
# - Verify with: python -c "import sys; print(sys.path)"
#
# COMMON ISSUES:
# - "ModuleNotFoundError" → PYTHONPATH not set or incorrect path
# - Imports work in one script but not another → Some scripts add sys.path manually
# - Works in terminal but not in debugger → VS Code not reading this .env
# ============================================================================

PYTHONPATH=./src
